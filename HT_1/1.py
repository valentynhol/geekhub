"""
1 .Write a script which accepts a sequence of comma-separated numbers from user
and generate a list and a tuple with those numbers.

"""


ListOfNum = list(input('Введіть дані: ').split(', '))
TupleOfNum = tuple(ListOfNum)

print(ListOfNum)
print(TupleOfNum)
